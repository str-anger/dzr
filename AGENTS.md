Generate code without comments and docstrings.
Generate code compatible with python 3.6.
Generate mimimal solution. Avoid checks if possible.
Prefer using flask, pytest, requests to analogs.
Limit line length to 80 characters.
In python always use "" instead on '' in strings.
Do not add empty lines within one function.
Remove trailing writespaces and lines with only whitespaces.
Avoid magic numbers and strings: if there is a path or a constant, create a const in the top of the file after imports.
